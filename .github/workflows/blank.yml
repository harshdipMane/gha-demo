name: Hellp world!
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "A demo build"
   test:
     runs-on: ubuntu-latest
     steps: 
       - name: test step
         run: echo "A test step"
