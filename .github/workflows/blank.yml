name: Hellp world!
on: 
 push:
   branches:
     - main
 pull_request:
   branches:
     - feature/github-action-on-running
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "A demo build"
  
  test:
     runs-on: ubuntu-latest
     needs: build
     steps: 
       - name: test step !
         run: echo "A test step"

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: deploy step !
        run: echo "A deploy step"
